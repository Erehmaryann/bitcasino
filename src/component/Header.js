import React from "react";

const Header = () => {
    return (
        <nav>
            <div>
                <img src="/assets/svg/logo.svg" alt="logo" />
            </div>
        </nav>
    );
};

export default Header;
