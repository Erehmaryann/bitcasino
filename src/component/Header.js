import React from "react";

const Header = () => {
    return (
        <header>
            <nav>
                <a href="/">
                    <img src="/assets/svg/logo.svg" alt="logo" />
                </a>
            </nav>
        </header>
    );
};

export default Header;
